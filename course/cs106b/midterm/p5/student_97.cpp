Vector<int> bestRoute(Grid<double> &distance, int startCity) {
	// I don't understand the sample solution, or how this is
	// supposed to work :/
for (int r = 0; r < distance.numRows() -  1; r ++){
	for (int c = 0; c < distance.numCols() -  1; c ++){
	}
}
