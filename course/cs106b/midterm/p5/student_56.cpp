Vector<int> bestRoute(Grid<double> &distance, int startCity) {
	if(){
		//basecase
		return //the vector containing the best route
	}
	//changes to the inputs so that the next round of recursion will be simpler and moves the problem towards the base case
	bestRoute(distance, startCity);
	return //some vector
}



//Must have three things:
//Can accept any input
//Has a base case
//Simplifies the problem towards the base case
