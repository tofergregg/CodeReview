Const DBL_MAX=100;

Vector<int> bestRoute(Grid<double> &distance, int startCity) {
	int totalTrip;
		for(int r=0;i<distance.numRows()){
			for(int c=0; j<distance.numCols());{
				totalTrip=+distance[r][startCity];
			}
		}
	//ran out of time
	
	
 double totalRouteDistance(Grid<double> &distance, Vector <int> &route);