string decode(string seq) {
	Stack<string> ops;
	int largest = seq.length() + 1;	//largest number
	//adding all the operations to the stack
	for(int i = 0; i < seq.length(); i++){
		ops.push(seq[i]);
	}
	
	
}
